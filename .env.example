# Server-side only. Never prefix with VITE_ and never commit a real key.
ANTHROPIC_API_KEY=
# Default model used by /api/ai. Override per deployment without code changes.
ANTHROPIC_MODEL_DEFAULT=claude-sonnet-5
